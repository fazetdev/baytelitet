'use client';

import React from 'react';
import { Clock, FileCheck, Landmark, Key, ChevronRight, AlertCircle } from 'lucide-react';

const activeDeals = [
  {
    id: 'D-902',
    property: 'Penthouse, Palm Jumeirah',
    client: 'Hans Mueller',
    value: '12,500,000 AED',
    stage: 'NOC',
    daysInStage: 4,
    health: 'On Track'
  },
  {
    id: 'D-905',
    property: 'Villa, Dubai Hills',
    client: 'Fatima Al-Suwaidi',
    value: '8,200,000 AED',
    stage: 'Security Deposit',
    daysInStage: 1,
    health: 'Urgent'
  }
];

const stages = [
 
const stageActions = {
  'Security Deposit': 'Collect 10% via escrow',
  'NOC': 'Submit to developer portal',
  'Trustee Office': 'Schedule DLD appointment',
  'Handover': 'Arrange key collection'
};
  { name: 'Security Deposit', icon: Clock },
 
const stageActions = {
  'Security Deposit': 'Collect 10% via escrow',
  'NOC': 'Submit to developer portal',
  'Trustee Office': 'Schedule DLD appointment',
  'Handover': 'Arrange key collection'
};
  { name: 'NOC', icon: FileCheck },
 
const stageActions = {
  'Security Deposit': 'Collect 10% via escrow',
  'NOC': 'Submit to developer portal',
  'Trustee Office': 'Schedule DLD appointment',
  'Handover': 'Arrange key collection'
};
  { name: 'Trustee Office', icon: Landmark },
 
const stageActions = {
  'Security Deposit': 'Collect 10% via escrow',
  'NOC': 'Submit to developer portal',
  'Trustee Office': 'Schedule DLD appointment',
  'Handover': 'Arrange key collection'
};
  { name: 'Handover', icon: Key }
 
const stageActions = {
  'Security Deposit': 'Collect 10% via escrow',
  'NOC': 'Submit to developer portal',
  'Trustee Office': 'Schedule DLD appointment',
  'Handover': 'Arrange key collection'
};
];
 
const stageActions = {
  'Security Deposit': 'Collect 10% via escrow',
  'NOC': 'Submit to developer portal',
  'Trustee Office': 'Schedule DLD appointment',
  'Handover': 'Arrange key collection'
};

export default function DealPipeline() {
  return (
    <div className="space-y-8">
      <div>
        <h2 className="text-2xl font-black italic uppercase tracking-tighter text-white">Transaction Pipeline</h2>
        <p className="text-gray-500 text-[10px] font-bold uppercase tracking-widest mt-1">Escrow & Transfer Tracking</p>
      </div>

      <div className="grid grid-cols-1 gap-6">
        {activeDeals.map((deal) => (
          <div key={deal.id} className="bg-white/5 border border-white/10 rounded-[2.5rem] p-8 hover:border-[#D4AF37]/30 transition-all">
            <div className="flex flex-col lg:flex-row justify-between gap-8">
              
              {/* Deal Identity */}
              <div className="space-y-2">
                <div className="flex items-center gap-2">
                  <span className="text-[10px] font-black bg-[#D4AF37] text-black px-2 py-0.5 rounded uppercase">Active Deal</span>
                  <span className="text-[10px] font-bold text-gray-500">ID: {deal.id}</span>
                </div>
                <h3 className="text-xl font-black text-white italic">{deal.property}</h3>
                <p className="text-sm text-gray-400 font-bold uppercase tracking-tight">Client: {deal.client}</p>
                <p className="text-[#D4AF37] font-black text-lg">{deal.value}</p>
              </div>

              {/* Visual Pipeline */}
              <div className="flex-1 max-w-2xl">
                <div className="flex justify-between items-center relative">
                  {/* Progress Line */}
                  <div className="absolute top-5 left-0 w-full h-[2px] bg-white/10 z-0"></div>
                  
                  {stages.map((stage, idx) => {
                    const isCurrent = deal.stage === stage.name;
                    const isPast = stages.findIndex(s => s.name === deal.stage) > idx;

                    return (
                      <div key={idx} className="relative z-10 flex flex-col items-center gap-3">
                        <div className={`w-10 h-10 rounded-full flex items-center justify-center border-2 transition-all ${
                          isCurrent ? 'bg-[#D4AF37] border-[#D4AF37] text-black scale-110 shadow-[0_0_15px_rgba(212,175,55,0.4)]' : 
                          isPast ? 'bg-white/10 border-[#D4AF37] text-[#D4AF37]' : 
                          'bg-black border-white/10 text-gray-600'
                        }`}>
                          <stage.icon className="w-5 h-5" />
                        </div>
                        <span className={`text-[9px] font-black uppercase tracking-tighter text-center max-w-[60px] ${
                          isCurrent ? 'text-white' : 'text-gray-500'
                        }`}>
                          {stage.name}
                        </span>
                      </div>
                    );
                  })}
                </div>
              </div>

              {/* Action/Alert */}
              <div className="flex flex-col justify-center items-end border-l border-white/10 pl-8">
                <div className={`flex items-center gap-2 mb-4 ${deal.health === 'Urgent' ? 'text-red-500' : 'text-green-500'}`}>
                  <AlertCircle className="w-4 h-4" />
                  <span className="text-[10px] font-black uppercase">{deal.health}</span>
                </div>
                <button className="p-4 bg-white/5 border border-white/10 rounded-2xl hover:bg-white/10 transition-all">
                  <ChevronRight className="w-5 h-5 text-[#D4AF37]" />
                </button>
              </div>

            </div>
          </div>
        ))}
      </div>
    </div>
  );
}
